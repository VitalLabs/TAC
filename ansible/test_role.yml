---
##Testing playbook for individual roles
##This assumes VPC was created and instances are running.
- name: Gather facts
  hosts: tag_Name_dev_app
  remote_user: deploy
  sudo: true
  vars_files:
  - vars/main.yml

  tasks: []

- name: test the role.
  hosts: tag_Name_dev_app
  remote_user: deploy
  sudo: true
  vars_files:
  - vars/main.yml

  roles:
  - app-clojure-config
