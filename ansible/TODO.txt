1 - VPC network ACLs and Security Groups
	- Port and route info (should be in docs now.)
2 - Remote user on DB plays is ubuntu, deploy on others?
3 - Break up vars for group_vars/all to be host group specific (dev_db, dev_app, etc.) ??
